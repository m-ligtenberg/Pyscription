# Core dependencies for Pyscription
requests>=2.28.0
pathlib2>=2.3.0; python_version < "3.4"

# Optional dependencies (graceful degradation if not available)
# These are imported with try/except blocks
# ollama>=0.1.0  # For local AI - optional, has fallback mode

# Development and testing dependencies (install with pip install -e .[dev])
# pytest>=7.0.0
# pytest-cov>=4.0.0
# flake8>=5.0.0
# black>=22.0.0
# mypy>=1.0.0

# Documentation dependencies (install with pip install -e .[docs])
# sphinx>=5.0.0
# sphinx-rtd-theme>=1.0.0

# Security dependencies (install with pip install -e .[security])
# bandit>=1.7.0
# safety>=2.0.0